{% extends "base.html.twig" %}
{% block body %}
    <h1> {{program.title}} </h1>
    <img src="{{program.poster}}">
    <p>
        Catégorie : {{ program.category.name }}
    </p>
    <p>  
        {{program.summary}}
    </p>
{% endblock %}
