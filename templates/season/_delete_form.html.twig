<form method="post" action="{{ path('season_delete', {'id': season.id}) }}" onsubmit="return confirm('Cette action est irréversible. Confirmer la suppression?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
    <button class="delete-btn">SUPPRIMER SAISON</button>
</form>
